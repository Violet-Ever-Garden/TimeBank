server:
  port: ${SERVER_PORT:8071}
spring:
  application:
    name: bus
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        file-extension: yml